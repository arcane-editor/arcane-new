// Unity Hierarchy panel — a live, read-only mirror of the connected Unity
// Editor's scene hierarchy (F-4.3). Backed by `useUnitySceneStore` (shared with
// the AI `@scene`/`@object` mentions). Public surface is the panel only.
export { HierarchyPanel } from './components/HierarchyPanel';
