# Dev-environment endpoints (public URLs only — never put secrets here).
# Local override: create .env.development.local (gitignored) — e.g. point
# VITE_API_URL=http://localhost:8787 at a local `wrangler dev`.
VITE_API_URL=https://api-dev.arcaneai.org
VITE_WEB_URL=https://dev.arcaneai.org
